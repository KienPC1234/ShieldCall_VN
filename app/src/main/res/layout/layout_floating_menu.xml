<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="200dp"
    android:layout_height="wrap_content"
    app:cardCornerRadius="16dp"
    app:cardElevation="8dp"
    app:cardBackgroundColor="@color/shield_surface"
    app:strokeWidth="1dp"
    app:strokeColor="@color/shield_outline">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_chat"
            style="@style/Widget.Material3.Button.TextButton.Icon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Chat AI"
            android:textAlignment="viewStart"
            android:textColor="@color/shield_on_surface"
            app:icon="@drawable/ic_launcher_foreground" 
            app:iconTint="@color/shield_primary"/>
            <!-- Using generic icon if specific ones don't exist yet, to be safe -->

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_record"
            style="@style/Widget.Material3.Button.TextButton.Icon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ghi âm"
            android:textAlignment="viewStart"
            android:textColor="@color/shield_on_surface"
            app:icon="@android:drawable/ic_btn_speak_now"
            app:iconTint="@color/shield_primary"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_screenshot"
            style="@style/Widget.Material3.Button.TextButton.Icon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Chụp màn hình"
            android:textAlignment="viewStart"
            android:textColor="@color/shield_on_surface"
            app:icon="@android:drawable/ic_menu_camera"
            app:iconTint="@color/shield_primary"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginVertical="4dp"
            android:background="@color/shield_surface_variant"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_hide"
            style="@style/Widget.Material3.Button.TextButton.Icon"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ẩn icon"
            android:textAlignment="viewStart"
            android:textColor="@color/shield_error"
            app:icon="@android:drawable/ic_menu_close_clear_cancel"
            app:iconTint="@color/shield_error"/>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>